#include<stdio.h>
int main()
{ 
  int n1=0,n2=1,next,n;
  printf("enter the number of fibonacci terms to generate:");
  scanf("%d",&n);

  printf("Fibonacci Series:");
  for(int i=1;i<=n;++i){
  printf("%d\n",n1);
  next=n1+n2;
   n1=n2;
   n2=next;
 } 
  return 0;
}
